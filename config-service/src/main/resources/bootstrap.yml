spring:
  application:
    name: config-service
          
management:
  endpoints:
    web:
      exposure:
        include: "*"
      base-path: /actuator
  endpoint:
    health:
      show-details: always
    restart:
      enabled: true

